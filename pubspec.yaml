name: flutter_example_users
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  rxdart: ^0.27.5
  get_it: ^7.2.0
  json_annotation: ^4.7.0
  dio: ^4.0.6

dev_dependencies:
  
  build_runner: ^2.2.1
  json_serializable: ^6.4.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true